<%= form_for(todoitem = @todoitem, html: {class:'ui form'}) do |f| %>
  <% if todoitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todoitem.errors.count, "error") %> prohibited this todoitem from being saved:</h2>

      <ul>
      <% todoitem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  	<%= f.text_area :result %>
  </div>
  <%= f.hidden_field :is_done, value: true %>
	<button class="ui icon teal button">
		<i class="check icon"></i>
		Done
	</button>
<% end %>
